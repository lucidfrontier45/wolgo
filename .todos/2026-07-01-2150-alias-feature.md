# TODO: 2026-07-01-2150-alias-feature

## Phase 1: Store + format

- [x] Add alias store layer — `targets.json` IO in `~/.wolgo/`, empty-registry fallback
- [x] Define registry format + validation — alias rules (no spaces/tabs, not MAC), MAC validate+normalize

## Phase 2: Resolution helpers

- [x] Add shared resolution helpers — MAC-beats-alias rule + all-targets enumeration

## Phase 3: Wire commands

- [x] Extend root wake — `--all` flag, accept alias or MAC, reject combo
- [x] Extend `find-ip` — `--all` flag, alias-aware, sorted `alias -> mac` for all
- [x] Add `register`/`list`/`remove` subcommands

## Phase 4: Docs

- [x] Update CLI help/long text — root + new subcommands + examples

## Phase 5: Verify

- [x] Add tests — registry, validation, resolve, list, remove, --all
- [x] Validate — `go test`, `go mod tidy`, `golangci-lint fmt`, `golangci-lint run`
